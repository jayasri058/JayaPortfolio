services:
  - type: web
    name: portfolio
    env: static
    buildCommand: npm install --legacy-peer-deps && npm run build
    staticPublishPath: build
    envVars:
      - key: CI
        value: false
      - key: NODE_VERSION
        value: 18.18.0
